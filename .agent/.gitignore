# The SQLite db is a fast, rebuildable CACHE — the committed source of truth is
# events.jsonl (append-only, merge-friendly). Do not commit the binary db; it
# would conflict across branches and is regenerated from events.jsonl on open.
journal.db
journal.db-wal
journal.db-shm
state/
snapshots/
indexes/
locks/
