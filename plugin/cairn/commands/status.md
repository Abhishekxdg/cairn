---
description: Show the current Cairn project state (goal, tasks, decisions, agents)
---

Run in the project root and present the output to the user:

```bash
cairn status
```

Summarize the goal, active tasks, active decisions and any live agents. If the user
asks "what changed?", follow up with `cairn timeline`.
