---
description: Show "where were we" — the current Cairn project context in one read
---

Run this in the project root and show the user the output verbatim:

```bash
cairn recall
```

This is the always-current orientation summary — goal, current task, active
decisions, anchored facts, recent activity, and the recommended next step. If the
command is not found, the repo has no Cairn journal yet; offer to run `cairn setup`.
